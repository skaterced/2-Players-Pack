#ifndef chess_h
#define chess_h

//#include <Arduino.h>
/*
      for (int i=0; i<NBCHESSPIECES; i++){
        if (pieces[i].i==selectedI){        // Piece is put down
          if // check if someone's already here
          pieces[i].i=selectorI;
          selected=false;
          selectedI=-1;
          break;  
        }
      //todo plein de truc
      }
      */
#endif

